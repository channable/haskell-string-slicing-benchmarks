module Lib
    ( naiveSlice
    , sliceWithRule
    , sequencedSlice
    , noInlineTakeSlice
    , reimplementedSlice
    ) where

import Lib.NaiveSlice (naiveSlice)
import Lib.SliceWithRule (sliceWithRule)
import Lib.SequencedSlice (sequencedSlice)
import Lib.NoInlineTakeSlice (noInlineTakeSlice)
import Lib.ReimplementedSlice (reimplementedSlice)
